# Ethereum Sepolia (chainId 11155111): canonical WETH / Circle testnet USDC.
ETHEREUM_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
# Historical mock deployment only. `local` always uses anvil dev keys.
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
# Maker: deploys Router / Guard, wraps WETH, funds taker, approves / ships / docks.
MAKER_PK=0x
# Taker: approves the router and swaps. `sepolia fund --execute` supplies initial demo funds.
TAKER_PK=0x
# LOCAL_RPC_URL=http://127.0.0.1:8545
# Read-only Ethereum mainnet price feeds; optional comma-separated failover endpoints.
# ETHEREUM_PRICE_RPC_URLS=https://ethereum-rpc.publicnode.com,https://eth.llamarpc.com
