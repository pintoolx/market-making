# Builder implementation status

Updated 2026-09-14. Scope: [approved goal v2](SWAPVM-BUILDER-IMPLEMENTATION-GOAL.md).
The full goal is user-paused; the separately authorized immediate follow-up is
consolidated in Draft [PR #102](https://github.com/pintoolx/market-making/pull/102)
on `feat/builder-cre-delivery`. Review only: no merge or Railway rollout until
explicitly authorized. See the [current checklist](BUILDER-REMAINING-PR.md) and
[verification record](BUILDER-INTEGRATION-VERIFICATION.md). Rows below describe
this branch; dated notes below them are historical, not current deployment proof.

**Correction:** nonzero LP fees undercount gross input at the current Guard.
Builder now enables zero fees only. The earlier PR #84 cap-safety claim was
incorrect; [reproduced evidence and mitigation](BUILDER-FEE-GUARD-LIMITATION.md)
explain the restriction and recovery of older artifacts.

| Area | Current state |
|---|---|
| Multi-turn domain | Strict drafts/patches, revision diffs/restoration, PostgreSQL persistence and actual agent implemented. Full intent/artifact requirement coverage remains pending |
| Capability inventory | Every non-reserved Aqua opcode accounted for; unsupported combinations retain specific reasons and distinct readiness evidence |
| Guard recipes | Extended existing V2 compiler to XYC/Pegged; CLMM retained; local actual-router tests pass |
| Compiler/decoder | Three zero-fee guarded curves compile deterministically. Independent decoder verifies bytes and still reads legacy fee artifacts for recovery; new nonzero-fee compilation is blocked |
| Deployment | Fixed-block read verifier refreshed at Sepolia finalized block 11691533; all 6 contracts, Guard/router bindings, token metadata, and USDC implementation address/runtime checked. Source-to-runtime and live recipe gates remain separate |
| Lifecycle simulation | Node adapter runs the exact compiled Maker artifact in an owned Sepolia fork. XYC/CLMM/Pegged each pass 25 named checks, with 11 successful swaps. Durable owned jobs/results, recovery/cancellation, API, eighth agent tool, separate process and Maker UI implemented. Explicit synthetic funding/report authority; no registration readiness. Rollout and remaining range/extreme-input coverage pending |
| PostgreSQL/auth | Additive migrations, owned drafts/messages/history, atomic idempotency/outbox, generic jobs, durable design turns, EOA SIWE sessions and HTTP handler. Privy app/user/session verification plus wallet proof implemented. The Railway mandate service now mounts the Builder handler and applies migrations at startup when `DATABASE_URL` is configured; database roles and least-privilege grants remain deployment work |
| OpenAI/UI | OpenAI + AI SDK twelve-tool multi-turn design and preparation, streamed durable events, Provider publication and Maker workspace. Actual model schema rejection fixed and three-turn capability/alternative/edit evaluation passes. Public lifecycle context separates runtime availability, consent and historical chain evidence |
| Private policies/template permissions | Signed immutable Provider versions, separate ciphertext storage, browser private editor and verified catalog, permission-checked Maker conversations and trusted relative-price resolution implemented. Maker standing-delivery consent is now an explicit signed, revision/artifact-bound action; private policy verification remains separate |
| Wallet/dynamic binding | Maker wallet preflight, explicit approve/ship/dock/Guard revoke/unrevoke/allowance revoke, durable request journal, canonical receipt and nonce/replacement/reorg recovery implemented. Browser + API + PostgreSQL + actual fork settlement passes. Funded public user-wallet acceptance remains pending |
| Event worker/recovery | Signed Provider/consent input, first evaluation, public Kraken and selected-contract EVM sources, durable health transitions, change-only two-phase local CRE delivery, nonce allocation, broadcaster fencing and stale-candidate refresh implemented. Consented outage pause and recovery pass real CLI + PostgreSQL evaluation; public-chain resident delivery/restart acceptance remains pending |
| Fees/modifiers | Nonzero LP fees blocked after actual local regressions proved per-swap gross input and post-inventory limit bypass. Zero-fee recipes remain enabled. Fee-aware Guard redesign, protocol/dynamic fees, decay and other unverified modifiers are deferred |
| Cloud/live acceptance/PRs | Earlier batches through PR #100 were integrated into main. PR #102 contains this follow-up and remains draft/unmerged by user instruction. Prior Railway rollout records below do not describe this branch. Full funded wallet, public-chain worker lifecycle and live CRE/TEE acceptance remain pending |

Current follow-up tests use disposable local databases. Real OpenAI requests
consume the user-configured key opaquely through a local process. New wallet
transactions execute only in owned local chains/forks. No new public-chain write,
Railway rollout, CRE deployment, activation or secret upload was performed.

## Historical milestones (not current readiness)

Core baseline verification: Builder 6 passed; executor suite 79 passed / 1 optional Sepolia fork skipped; both TypeScript checks pass. After making exported order traits JSON-safe, the 5 Builder compiler/settlement cases were rerun successfully. A pinned-action CI workflow runs the domain and local Guard suites without secrets or public-chain writes. Later batches and their additional tests are described below.

Keep remaining full-goal acceptance pending as listed above. The original workspace remains on another user's ENS work; do not overwrite it or assume its files match this implementation worktree.

The latest main also contains ENS/monitor/standing-documentation updates and a publication config fix; all are retained. After integrating main, orchestrator verification was 43 passed / 1 optional fork skipped. Two pre-existing interrupted-transaction tests were fixed to compare pending nonces when the transaction is still in Anvil's mempool; all 12 affected tests and final CI passed.

Design follow-up: [service package](../packages/builder-service/README.md) and [live evaluation evidence](BUILDER-AGENT-EVALUATION.md). Current local service verification: 91 tests passed and one optional network case skipped on PostgreSQL 16.15; TypeScript passed; CI targets 18.6. Lease/cancellation/restart, public event replay, per-subscription serialization, numeric history ordering, Date-to-JSON conversion, source backfill/reorg, stale delivery blocking, pending nonce ordering, broadcast receipt reconciliation and provider error redaction are covered. The model workflow prepares public drafts, permission-checked Maker instances, compiled artifacts and background simulations. Provider private-policy editing stays in a separate browser form; live Guard report delivery and funded wallet confirmation remain required.

Workspace follow-up: `/builder` now provides public strategy chat, streamed replies, current parameters/validation, version diffs/restoration, cancellation and recovery. Python Playwright passed real API/PostgreSQL tests using fixture Privy identity and a deterministic model, including lost responses, expired sessions and mobile layout. Production Next build and scoped lint passed. Provider Studio now exposes a seventh, always-visible Custom design card linking to `/builder`; live Privy/browser acceptance and service rollout remain pending. Entry visibility does not establish backend readiness.

Latest-main integration retains wallet-backed taker trading, ENS strategy discovery and the shared page scroll fix from PRs #50/#51. The browser fixture also checks that the mobile user can scroll to version controls and return to the heading.

Workspace PR #53 merged at `9b4253b` after final-head PostgreSQL and Guard CI passed, along with local production build and browser acceptance. Seven additional real OpenAI turns now verify incremental limits, XYC → Pegged → CLMM changes, unchanged constraints and conflict discussion without a mutation. Partial drafts retain missing fields explicitly and remain blocked from compile; direct API patches also reject pair relabeling after amounts or price intent exist. The public compiler is separated from artifact-loading/wallet imports and exported as `aqua-executor/builder`.

Compilation follow-up: migration 004 persists immutable decoded Maker artifacts, deduplicated by revision/manifest and protected by ownership and agent leases. API compile/list/get and the seventh `compileStrategy` agent tool are wired. Reads mark old revisions/restores/manifests stale and always keep registration readiness false pending simulation and wallet gates. Local service suite 28 passed; domain 8 passed; executor 81 passed / 1 optional fork skipped. Provider template preparation and remaining tools are still required.

Actual model compiler acceptance: `eval-design-api.ts --maker-compile` passed five turns through signed HTTP/API/durable worker and real compiler artifacts. It verifies XYC → cap edit → Pegged → CLMM, exact decoded caps, one current artifact and stale predecessors. Allocations are public local fixture inputs; no wallet inventory, public-chain write or lifecycle simulation is claimed. Browser acceptance also passed an initial single-cap draft with missing limits visibly retained.

Latest main includes PR #54’s explicit CRE market-scenario runner and fixture-source guards. These changes are retained; Builder lifecycle simulation has not yet been wired to that runner.

Preparation PR #55 merged at `d759691` after final-head Guard/PostgreSQL CI and Cloudflare preview passed. The standalone [lifecycle evaluator](BUILDER-LIFECYCLE-SIMULATION.md) is the next batch. Its snapshot/receipt handling, exact cap boundaries, report domain/replay/active-switch behavior, wallet/virtual exhaustion, deadline/standing/revoke/dock checks and evidence modes are documented separately. Full goal completion still requires the pending product and deployment gates above.

Lifecycle PR #60 merged at `c3beac8` after exact-head review, GitHub Guard/PostgreSQL CI and Cloudflare preview passed. The integrated main retains PRs #57–#59. Current simulation-service work adds migration 005 and a dedicated worker CLI, with 38 default service tests passing and one optional network case skipped. The separate-process real fork test passed independently. The final live model evaluation passed five turns: enqueue/read success → one-atomic-unit failure → cap restoration and a fresh successful fork → scoped result explanation. This is public, local fixture evidence; Railway business migrations, product mounting and actual wallet/CRE delivery remain pending.

Simulation jobs [PR #63](https://github.com/pintoolx/market-making/pull/63) merged at `4f8bcfd` after exact-head review, PostgreSQL CI and Cloudflare success; main PRs #61/#62 remain integrated. The next [scenario preview batch](BUILDER-SCENARIO-PREVIEWS.md) adds migration 006, immutable revision-bound mathematical results and the ninth tool. All 27 settlements across nine balanced/unequal/reversed configurations match the integer preview and quote; five real OpenAI Provider turns preserve constraints, observe output-token cap refusals and compare all three curves. Current service suite: 44 passed / one optional network skip; both package typechecks pass. This does not complete inventory, Maker/product UI, publication, event delivery or rollout.

Numerical preview [PR #64](https://github.com/pintoolx/market-making/pull/64) merged at `59cecd5` after exact-head Guard/PostgreSQL CI and Cloudflare success. The next [inventory batch](BUILDER-WALLET-INVENTORY.md) adds the tenth tool and owned, revision-checked current wallet reads. Native Sepolia reads, five shared-inventory fork snapshots and three real OpenAI inventory turns pass. Service suite: 49 passed / one optional network skip; both typechecks pass. No business migration is needed for these on-demand reads; transaction-bound context storage remains part of wallet planning. Dedicated Maker controls, the final two preparation tools, publication, event delivery and rollout remain pending.

Inventory [PR #65](https://github.com/pintoolx/market-making/pull/65) merged at `2b43f0a` after exact-head Guard/PostgreSQL CI and Cloudflare success. The [Provider template batch](BUILDER-PROVIDER-TEMPLATES.md) adds migration 007, signed immutable versions, separate ciphertext storage, permanent withdrawal and Maker instances with original-baseline permission checks. Relative CLMM uses a trusted bounded market-proxy adapter; caller snapshots are rejected. Domain: 12 tests passed. Service: 59 passed / one optional network skip. Four real OpenAI permission/edit/refusal/restore turns and a real Kraken price observation passed. Private editor/browser publication, wallet plans, consent/event delivery, fees and platform rollout remain pending.

Provider API [PR #66](https://github.com/pintoolx/market-making/pull/66) merged at `66106f2` after exact-head Guard/PostgreSQL CI and Cloudflare success. The [template workspace batch](BUILDER-TEMPLATE-WORKSPACE.md) connects browser-only ordered private rules, locally verified wallet publication, public version catalog, Maker application and pinned conversation controls. Both browser acceptance suites passed, including lost-response retries, single-signature publication, private clearing, intent/session expiry and withdrawal. Actual browser ciphertext passes the existing decryptor/schema/evaluator in an isolated fixture; no TEE is claimed. Domain: 14 passed; public signature review: 2 passed; service: 59 passed / one optional network skip. Production Next build, all affected typechecks and scoped lint passed. No business database migration or public-chain transaction was performed. Wallet plans, requirement acceptance, consent/event delivery, fees and rollout remain required.

Template workspace [PR #67](https://github.com/pintoolx/market-making/pull/67) merged at `664aba1` after exact-head Guard/PostgreSQL CI and Cloudflare success. The [Maker preparation batch](BUILDER-MAKER-PREPARATION.md) exposes existing inventory/compile/simulation APIs, with current-revision binding, historical evidence, explicit mock/fork/live labels, background cancellation and lost-response recovery. All three browser suites pass against real local PostgreSQL and compiler with synthetic inventory/settlement adapters. Four public browser unit tests, typecheck, lint and production Next build pass. A dedicated CI browser job now reproduces these checks. Wallet plans, requirement acceptance, consent/bindings/event delivery, fees and platform rollout remain required.

**Superseded safety claim: see the 2026-09-14 correction above.** Fixed-fee follow-up [PR #84](https://github.com/pintoolx/market-making/pull/84) merged at `9a2df9c`. Guard V2 now composes the pinned fixed input-fee opcode with XYC, concentrated and Pegged recipes; tests cover gross input credited to Aqua, net curve pricing, rounded fees, both directions and sequential local settlement. Guard V1, protocol/dynamic fees and other modifiers remain intentionally restricted. The Railway deployment and `/health` endpoint were verified after merge; no public-chain write or CRE deploy was performed.

Event-source follow-up [PR #86](https://github.com/pintoolx/market-making/pull/86) merged at `6e9ff0b`. It adds `createEvmLogSource` and connects it to the durable worker. Confirmed block ranges are chunked through viem, cursors and bounded event-bearing block hashes are persisted in `builder.event_cursors`, and a changed block hash marks the old inbox identities reorged before replay. The source is opt-in through `BUILDER_EVENT_EVM_ADDRESS` plus `BUILDER_EVENT_EVM_FROM_BLOCK`; evaluator and delivery adapters remain required for any Guard report. Local PostgreSQL/event tests and Builder typecheck pass; no public-chain write or CRE deploy was performed.

Event-health follow-up [PR #87](https://github.com/pintoolx/market-making/pull/87) merged at `0dc0f56`. Signed health ingress now passes strict validation, authenticated Maker sessions can read source/chain/observation/health summaries, and the preparation view shows monitoring separately from standing authorization. The Railway deployment `fafe30f3-00a4-45e3-a69f-4e5a08578baf` is healthy and `/health` reports Ethereum Sepolia. No public-chain write or CRE deploy was performed.

Broadcast receipt recovery [PR #89](https://github.com/pintoolx/market-making/pull/89) merged at `3e5e8d9`. The resident event worker now enumerates persisted `broadcast` rows and reconciles their receipts after restart before retrying any pending report; unresolved rows remain broadcast and are never blindly sent again. PostgreSQL, browser, English-copy and Cloudflare checks passed. Railway deployment `8422fa00-f267-43f1-a95f-e27b8a52bb93` is healthy and `/health` reports Ethereum Sepolia. No public-chain write or CRE deploy was performed.

Event concurrency [PR #91](https://github.com/pintoolx/market-making/pull/91) and delivery ordering [PR #93](https://github.com/pintoolx/market-making/pull/93) merged at `a7353ce` and `3bcea3b`. The worker now cancels unsent work on stop or switch, serializes active evaluation per subscription, verifies the current generation/binding before delivery and holds later pending reports behind earlier unresolved nonces. PostgreSQL, browser and English-copy checks passed; no public-chain write or CRE deploy was performed.

Lifecycle cancellation [PR #95](https://github.com/pintoolx/market-making/pull/95) merged at `93f1a42`. A shared transaction helper now cancels pending/running evaluations and pending report deliveries when a subscription is stopped or its draft, consent or Provider template lifecycle changes. Reports already marked `broadcast` remain available for receipt reconciliation. The service suite passes 82 tests with one optional fork skipped; PostgreSQL, browser, English-copy and Cloudflare checks passed. Railway deployment `55d6435a-a6fe-4fdb-8291-d70ab98e6658` is healthy; no public-chain write, CRE deployment or activation was performed.

Paused-consent cancellation [PR #97](https://github.com/pintoolx/market-making/pull/97) merged at `c04633c`. Revoking Maker automation consent now stops both enabled and paused subscriptions and cancels their unsent work. The event regression suite covers a paused subscription with an unbroadcast report; all required checks passed and no public-chain write or CRE deployment was performed.

CRE HTTP trigger compatibility [PR #98](https://github.com/pintoolx/market-making/pull/98) merged at `75dbfd1`. The confidential workflow HTTP entry point now supports the event worker's pre-provisioned Maker request (`maker` plus `strategyHash`) without requiring a browser envelope, while preserving sealed dynamic Maker policies and wallet binding. Workflow tests pass 60 cases, the synthetic leak scan passes, and a staging CLI simulation completed in dry-run with a public Guard report summary. This is simulator evidence only; no CRE deployment, activation or public-chain write was performed. Railway deployment `0d5dfe76-e01c-49ff-ab56-c6ead7504a5a` is healthy and `/health` reports Ethereum Sepolia.

Event delivery review [PR #100](https://github.com/pintoolx/market-making/pull/100) includes source filtering, owner-scoped notification deduplication, direct-send ordering across subscription generations, and nonce/report-transition fixes. Service verification passes 85 tests with one optional fork skipped; all three local browser acceptance suites pass. GitHub PostgreSQL/browser/English and Cloudflare checks passed for implementation commit `6b3dabf`. A private custom-format backup of the production Builder schema was created before migrations 014–015, copied outside the database volume with file mode 0600, and fully decoded by PostgreSQL 18.6 tooling. Native Railway backup creation returned unauthorized, so the backup used an authenticated temporary SSH connection instead. Migration 015 rollback constraints are documented in [platform wiring](BUILDER-PLATFORM-WIRING.md).

At that milestone, remaining event acceptance included per-Maker/strategy routing, source-outage consented pause/recovery and durable gateway identity/fencing. PR #102 now implements these paths with local evidence; public-chain acceptance remains pending. The stored binding nonce is a verified baseline; the delivery gateway still needs current Guard readback and broadcaster coordination. These gaps and live CRE/Guard/funded-wallet acceptance remain part of the full goal; passing the queue tests does not enable production event delivery.

Event recovery follow-up retains ambiguous delivery outcomes in `broadcast`, shares a durable delivery ID with the gateway and records terminal not-broadcast/reverted reconciliation. Migration 016 preserves orphaned evaluation history, cancels unsent orphan-derived reports and creates new evaluation identities on re-inclusion. Short inbox locks serialize reorg updates with evaluation completion and broadcast claims; already-broadcast reports remain reconcilable. Regression tests reproduced both defects before the fix. The service suite passes 91 tests with one optional fork skipped, including an upgrade from 015 with existing pending/broadcast/accepted records; typecheck passes. Required gateway fencing and pre-016 rollback restrictions are documented in platform wiring. This evidence is local; live gateway acceptance is still required.
