# Flat, npm-like node_modules. This codebase was built under npm's hoisted
# layout; keep it hoisted so transitive/peer resolution keeps working under pnpm.
node-linker=hoisted
# Solana SDKs ship mismatched peer ranges; don't fail installs on them.
strict-peer-dependencies=false
